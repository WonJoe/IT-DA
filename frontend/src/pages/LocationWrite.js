import React from 'react';
import './LocationWrite.css';
import api from '../API_KEY';

const LocationWrite = () => {
    return (
        <div>
            {api.api}
        </div>
    );
};

export default LocationWrite;